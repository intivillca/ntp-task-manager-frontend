// Contex for app translation
// Contains : 2 lang packs and functions to switch from one to other

import {createContext} from 'react';

const languageContext = createContext();

export default languageContext;